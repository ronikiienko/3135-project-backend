# rrrrrrrrrrrrrrrrrrrrr

To start project:

```
npm install
npm run start
```

This will start a parcel dev server (url will be logged to the console)

If the page looks stale (Parcel sometimes caches builds), remove the `.parcel-cache` and `dist` folders and restart the dev server.
